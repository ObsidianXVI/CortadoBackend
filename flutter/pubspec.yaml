name: cortado
description: Placeholder Flutter package for the Cortado cloud IDE backend client.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  grpc: ^4.0.0
  protobuf: ^3.1.0
  web_socket_channel: ^3.0.0
  riverpod: ^2.5.0
  freezed_annotation: ^2.4.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  freezed: ^2.4.0
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: false
