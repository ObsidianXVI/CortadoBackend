apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: cortado-workspace
  labels:
    app.kubernetes.io/part-of: cortado
provisioner: pd.csi.storage.gke.io
parameters:
  type: pd-balanced
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
